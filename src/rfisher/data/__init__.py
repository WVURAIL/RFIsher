"""Versioned runtime data distributed with RFIsher.

Use :mod:`rfisher.resources` to access these files. Keeping them in a
package makes the same data available from a source checkout, wheel, or sdist
without relying on the current working directory or a repository layout.
"""
